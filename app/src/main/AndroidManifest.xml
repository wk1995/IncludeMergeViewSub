<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wk.includeMergeViewsub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".include.IncludeSimpleNormalActivity" />
        <activity android:name=".include.IncludeIdErrorActivity" />
        <activity android:name=".include.IncludeLayout_Activity" />
        <activity android:name=".merge.MergeFrameLayoutActivity" />
        <activity android:name=".merge.MergeMergeActivity" />
        <activity android:name=".merge.MergeIncludeActivity" />
        <activity android:name=".merge.MergeIncludeMergeActivity" />
        <activity android:name=".ViewSub.ViewSubActivity"></activity>
    </application>

</manifest>